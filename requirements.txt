pandas>=1.3.0

# ==============================================================================
# NumPy & joblib: PINNED FOR REPRODUCIBILITY
# ==============================================================================
# NumPy 2.x introduced breaking changes to internal module paths (numpy._core),
# causing incompatibility with pickled objects (preprocessors, sklearn pipelines)
# serialized under NumPy 1.x. This is a known reproducibility issue in research.
#
# Pinning to NumPy 1.26.4 ensures:
# 1. Consistent serialization/deserialization of ML artifacts
# 2. Reproducibility across training and inference environments
# 3. Compatibility with joblib-serialized preprocessing pipelines
#
# Academic Rationale: Research-grade systems require deterministic artifact
# loading. NumPy version mismatches break reproducibility and prevent
# independent verification of results.
# ==============================================================================
numpy==1.26.4
joblib==1.4.2

tensorflow>=2.13.0,<3.0.0
flwr[simulation]>=1.5.0
scikit-learn>=1.0.0
matplotlib>=3.3.0
seaborn>=0.11.0
flask>=2.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pdfplumber>=0.9.0
pytesseract>=0.3.10
Pillow>=9.0.0
